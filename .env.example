# Environment Configuration Template
# Copy this file to .env and modify the values as needed

# Camera Configuration
# Comma-separated list of camera sources
# Use camera index (0, 1, 2) for local cameras
# Use RTSP URLs for network cameras
# CAMERA_STREAMS=0,rtsp://username:password@192.168.1.100:554/stream1

# YOLO Model Configuration
# MODEL_NAME=yolo11n.pt
# TENSORRT_MODEL_NAME=yolo12x.engine
# SEGMENTATION_MODEL_NAME=yolo11n-seg.pt
# TENSORRT_SEGMENTATION_MODEL_NAME=yolo11m-seg.engine
# MODEL_CONFIDENCE=0.5
# MODEL_IOU_THRESHOLD=0.45

# Display Configuration
# WINDOW_WIDTH=640
# WINDOW_HEIGHT=480
# DISPLAY_FPS=true

# GPU Configuration
# USE_GPU=true
# DEVICE=cuda

# Performance Configuration
# FRAME_BUFFER_SIZE=1
# RTSP_TIMEOUT=10

# Traffic Analysis Zone Configuration
# Entry zones configuration (JSON format)
# Each zone is defined as an array of [x, y] coordinate pairs
# Example: Three entry zones
ZONE_IN_POLYGONS="[
    [[806, 121], [1390, 121], [1390, 77], [806, 77]], 
    [[690, 300], [1530, 300], [1530, 230], [690, 230]],
    [[271, 681], [520, 681], [520, 450], [271, 450]]
    ]"


# Example RTSP URLs (replace with your camera details)
# Generic RTSP: rtsp://username:password@ip:port/path
# Hikvision: rtsp://admin:password@192.168.1.100:554/Streaming/Channels/101
# Dahua: rtsp://admin:password@192.168.1.101:554/cam/realmonitor?channel=1&subtype=0
# Axis: rtsp://user:password@192.168.1.102/axis-media/media.amp
# Foscam: rtsp://admin:password@192.168.1.103:88/videoMain
